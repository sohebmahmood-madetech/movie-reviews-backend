# JWT Keys for Review Authentication (RS512 Algorithm)
# Generate your own RSA keypair for production use
# You can generate keys using: openssl genrsa -out private.pem 2048 && openssl rsa -in private.pem -pubout -out public.pem
# Then base64 encode them for use here

# Base64-encoded RSA private key for JWT signing
MOVIEREVIEWS_AUTH_REVIEW_PRIVATE_KEY=your_base64_encoded_private_key_here

# Base64-encoded RSA public key for JWT verification
MOVIEREVIEWS_AUTH_REVIEW_PUBLIC_KEY=your_base64_encoded_public_key_here

# Database Configuration (if using external database)
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/moviereviews
# SPRING_DATASOURCE_USERNAME=your_db_username
# SPRING_DATASOURCE_PASSWORD=your_db_password

# Application Security Settings
# MOVIEREVIEWS_SECURITY_RATE_LIMIT_REQUESTS_PER_MINUTE=60
# MOVIEREVIEWS_SECURITY_RATE_LIMIT_BURST_CAPACITY=10